if OPT_REF_IMPL
  STEM_KERNEL = stem_kernel
endif
if OPT_STRING_KERNEL
  STRING_KERNEL = string_kernel
endif
if OPT_SIMPAL
  SIMPAL = simpal
endif
SUBDIRS =  common libsvm \
	$(STRING_KERNEL) $(STEM_KERNEL) $(SIMPAL) stem_kernel_lite rla_kernel
